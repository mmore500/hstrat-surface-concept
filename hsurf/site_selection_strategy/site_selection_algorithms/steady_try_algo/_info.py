import typing

def allows_discard() -> bool:
    return False


def calc_rank_bound(surface_size: int) -> typing.Optional[int]:
    """What is the first unsupported rank?"""
    return None
    supported ranks limit bound
